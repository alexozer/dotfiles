#! /bin/sh

lastdesk=$[$(bspc query -m -D | wc -l)%10]

if [[ "$1" == "add" ]]; then
	[[ $lastdesk -eq 0 ]] && exit
	bspc monitor -a $[(lastdesk+1)%10]
elif [[ "$1" == "remove" ]]; then
	bspc monitor -r $lastdesk
fi

exit
