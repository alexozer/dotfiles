#!/bin/bash

fifopath=/tmp/kbdhotplugd.fifo
[ -e "$fifopath" ] || mkfifo "$fifopath"

echo line > "$fifopath"&

tail -f "$fifopath" --pid=$$ |\
    while read -r line; do
        setxkbmap -option caps:escape -option shift:both_capslock
    done
